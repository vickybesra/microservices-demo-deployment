# ECR Repositories

This creates ECR repos for Docker images.

## What it does
- Makes AWS ECR repositories for each microservice.

## How to use
- Run `terraform init && terraform apply` to create them.
- Use the repo URLs in Jenkins pipelines.
